# Copy to .env and fill in real values. .env is git-ignored.
# docker compose reads this file automatically; each variable is passed to the
# matching service via its `environment:` block in docker-compose.yml.

# remote-rocketship
RR_API_KEY=your_remoterocketship_api_key_here
